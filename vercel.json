{
  "buildCommand": "npm --prefix my-portfolio install && npm --prefix my-portfolio run build",
  "outputDirectory": "my-portfolio/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
